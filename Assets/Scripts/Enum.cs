public enum PlayerType
{
    Player1, Player2, Player3, Player4, Player5, Player6,
}
public enum State
{
    Idle, moving, Shooting, Dead
}
public enum GameState
{
    Playing,
    Paused,
    GameOver
}